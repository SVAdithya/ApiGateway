# Spring Boot application properties for rate-limiter
server.port=8081

# Redis configuration
spring.data.redis.host=localhost
spring.data.redis.port=6379

# Rate limit configuration
ratelimit.requests=10
ratelimit.seconds=60
